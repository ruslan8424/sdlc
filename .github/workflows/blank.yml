name: Deploy VPC


on:
  push:
    branches: [ "main" ]



jobs:
  build:
    runs-on: ubuntu-latest #windows-latest #mac-latest
    steps:
    
      - uses: actions/checkout@v4

      - name: Initialize terraform
        working-directoru: vpc
        run: |
          terraform init


      - name: Configur workspace
        working-directoru: vpc
        run: |
            terraform workspace new dev || terraform workspace select dev

  
      - name: Plan
        working-directoru: vpc
        run: |
                terraform plan          